# === HOST PORTS (override to avoid conflicts) ===
HOST_CLIENTES_PORT=8000
HOST_CATALOGO_PORT=8001
HOST_FACTURACION_PORT=8002
HOST_PAGOS_PORT=8003
HOST_ORQ_PORT=8010
HOST_PORTAL_PORT=8088
HOST_BACKOFFICE_PORT=8089
HOST_WHATSAPP_PORT=8011
HOST_INSTALACIONES_PORT=8005
HOST_RED_PORT=8020
HOST_TICKETS_PORT=8006

# === DATABASES ===
DB_HOST=infra-postgres
DB_USER=isp_admin
DB_PASS=admin
DB_NAME=isp_mvp
POSTGRES_URL=postgresql://isp_admin:admin@localhost:5432/isp_mvp
REDIS_URL=redis://localhost:6379
KAFKA_BROKERS=localhost:9092

# === OBSERVABILITY ===
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_JAEGER_ENDPOINT=http://localhost:14268/api/traces
PROMETHEUS_URL=http://localhost:9090

# === AUTH (KEYCLOAK) ===
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=telecable
KEYCLOAK_CLIENT_ID=isp-web
KEYCLOAK_CLIENT_SECRET=dev-secret
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# === OIDC (apps) ===
OIDC_ISSUER=http://localhost:8080/realms/telecable
OIDC_CLIENT_ID=isp-web
OIDC_CLIENT_SECRET=dev-secret
OIDC_AUDIENCE=isp-web

# === INTEGRATIONS ===
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=admin123
MINIO_BUCKET=facturacion
STRIPE_KEYS=test-key
WA_TOKEN=whatsapp-test-token
WA_PHONE_ID=0000
ROUTER_MODE=emulated
PAYMENT_MODE=emulated
KAFKA_BROKER=infra-kafka:29093
